import React, { Component } from 'react'
import SellCarousel from '../Carousel/SellCarousel'

export default class SellFrontPage extends Component {
    render() {
        return (
            <div>
              <SellCarousel/>
            </div>
        )
    }
}
